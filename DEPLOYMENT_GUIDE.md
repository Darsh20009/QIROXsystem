# QIROX Studio — دليل نشر آمن

## قاعدة الأسرار

لا تُحفظ مفاتيح Apple أو Google أو الدفع أو MongoDB داخل المستودع أو ملفات
الإعداد أو لقطات الشاشة. تُضاف القيم فقط إلى Replit Secrets أو مدير أسرار
مزود البناء، وتُستخدم أسماء المتغيرات دون لصق قيمة السر في التوثيق.

## إعدادات Apple

أضف متغيرات App Store Connect وSign in with Apple وAPNs إلى مدير الأسرار
المناسب. يجب أن تكون صلاحيات المفاتيح أقل ما يلزم، مع تدوير أي مفتاح ظهر
سابقاً في Git أو السجلات.

## إعدادات التطبيق

يجب ضبط `MONGODB_URI` و`SESSION_SECRET` و`SANDBOX_ENC_KEY` من Replit Secrets.
في الإنتاج يتوقف الخادم إذا غابت الأسرار الإلزامية، ولا توجد قيم احتياطية
داخل الكود.

## قبل النشر

شغّل:

```bash
npm run security:check
npm run build
```

ثم راجع أن ملفات الأسرار غير متتبعة:

```bash
git ls-files | grep -Ei '(^|/)(\.env|.*\.(pem|p8|key|p12))$'
```

لا تُسجّل قيم الأسرار، ولا تضعها في issue أو pull request أو ملف توثيق.