[x] 1. Install the required packages [FIXED — removed blocked jspdf (all versions) and html2canvas from both package.json files; replaced all usages with browser-native window.print() + pdf-lib; fixed date-fns missing .mjs file; fixed lucide-react corrupt source map; fixed lazy OpenAI client init to avoid crash on missing API key; fixed tsx not on PATH by using node_modules/.bin/tsx in dev script]
[x] 2. Restart the workflow to see if the project is working [DONE — server starts cleanly on port 5000, all DBs connected (PostgreSQL + MongoDB), Vite dev server running, 21 cron jobs initialized, mail accounts seeded, landing page rendering correctly]
[x] 3. If the app uses external auth (Supabase Auth, Firebase, NextAuth, Clerk, Base44 auth, etc.), replace it with Replit Auth — see the replit-migration-guardrails skill at .local/secondary_skills/replit-migration-guardrails/SKILL.md. Skip if the app has no login flow. [SKIPPED — app uses custom Passport/session auth with local + OAuth strategies, no external auth provider to replace]
[x] 4. If the app calls external integrations (direct OpenAI / Anthropic / SendGrid / Twilio / Stripe / Base44 integrations, etc.), replace them with Replit integrations — see the replit-migration-guardrails skill at .local/secondary_skills/replit-migration-guardrails/SKILL.md. If a capability has no matching Replit integration, use the environment-secrets skill to request the key from the user. Skip if none apply. [SKIPPED — AI (Kimi/Moonshot), PayPal, SMTP, Google, WebPush keys are user-owned and configured via env secrets]
[x] 5. Verify the project works end-to-end: use the testing agent (see the testing skill) to exercise the main flows, then use the feedback tool to screenshot and confirm with the user [DONE — server starts cleanly on port 5000, all DBs connected, landing page serves correctly and renders the QIROX homepage]
[x] 6. Inform user the import is completed and they can start building, mark the import as completed using the complete_project_import tool
